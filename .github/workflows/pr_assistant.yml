name: PR Assistant

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  summarize:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR assistant stub; passing."
